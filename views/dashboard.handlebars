<div class="col  ">
  <h2>MyDashboard : <span>  </span>   {{user_name}} </h2>
  <div class="col" style="padding: 0 auto;">
    {{#each blogs as |blog| }}
        {{> blogdash-details logininfo=../logininfo}}
    {{/each}}

    {{#each comments as |comment|}}
      {{>comment-details logininfo=../logininfo}}
    {{/each}}

  </div>
</div>